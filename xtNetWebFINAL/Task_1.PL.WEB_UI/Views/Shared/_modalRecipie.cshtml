@model Task_1.PL.WEB_UI.Models.Recipie.RecipieVM

<div class="container">
    <div class="form-group">
        <h3>@Model.Title</h3>
    </div>
    <div class="form-group">
        @Model.Description
    </div>
    <div class="form-group">
        <img width="100" height="100" class="" src="@Url.Action("Get","Image", new { Id = Model.Image_id })" />
    </div>
</div>
